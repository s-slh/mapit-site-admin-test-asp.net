namespace MapitSiteAdmin.Constants.Users
{

    public enum Roles
    {
        //These are the default roles that we would try to seed to the database
        //as soon as the application fires up.
        SuperAdmin,
        Admin,
        Basic
    }
}
